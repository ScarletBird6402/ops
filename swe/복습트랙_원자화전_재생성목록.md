# 복습 트랙 — 원자화 전 학습분 재생성 목록

> 처음 목차(원자화 전)는 지금까지 **실무에서 실제로 다룬** 주제들이다. 이를 최종 코드 체계(A~Z)에 매핑한, **복습용으로 다시 생성할 대상** 목록.
> 우선순위 낮음: 신규 실무 발원 항목이 항상 먼저다. 여력이 있을 때 **틈틈이** 한두 개씩 재생성해 복습한다.
> 총 80개 항목. 체크: `[ ]` 미생성 / `[x]` 재생성·게시 완료.

## A. 프론트엔드
- [ ] **A1** 컴포넌트 통신 방식 결정
- [ ] **A2** Vue 반응성 시스템 원리
- [ ] **A3** 컴포넌트 생명주기와 정리
- [ ] **A4** 이벤트 버스(Pub/Sub)와 메모리 누수
- [ ] **A5** 조건부 렌더링 전략 — DOM 제거 vs 숨김 — 확장됨(범위 넓어짐, 복습 시 새 관점 추가)
- [ ] **A6** 양방향 바인딩의 실체 — 확장됨(범위 넓어짐, 복습 시 새 관점 추가)
- [ ] **A7** provide/inject와 prop drilling 회피
- [ ] **A8** Composable 로직 재사용
- [ ] **A9** 상태 관리 — 언제 전역으로
- [ ] **A10** 렌더링 성능
- [ ] **A11** 접근 제어(권한) UX 3패턴

## C. 백엔드
- [ ] **C1** 서버사이드 렌더링 vs 클라이언트 렌더링
- [ ] **C2** JSP 실행 모델
- [ ] **C3** 클라이언트 vs 서버사이드 리다이렉트
- [ ] **C4** 서버 세션 관리
- [ ] **C5** SSO와 SAML 인증 흐름
- [ ] **C6** SSR·SPA 통합
- [ ] **C7** 어댑터 패턴 — 인터페이스 변환
- [ ] **C8** 디스패처 패턴과 method 분기
- [ ] **C9** 분기 구현 전략 비교
- [x] **C10** 직렬화와 역직렬화
- [x] **C11** 마샬링과 타입 시그니처 매핑
- [ ] **C12** FFI 개요 — 언어 경계 호출
- [ ] **C13** FFI 방식 비교
- [ ] **C14** C++ 경계의 메모리 수명·예외 전파
- [ ] **C15** 트랜잭션 관리와 전파
- [ ] **C16** 예외 처리 전략

## D. API·통신·메시징
- [x] **D2** API 설계 기초

## E. 데이터베이스
- [x] **E1** 인덱스 원리와 복합 인덱스
- [ ] **E2** 정규화 vs 역정규화 — 파생 컬럼
- [x] **E3** 트랜잭션 기초와 ACID — 분리됨
- [x] **E4** 격리 수준과 락 — 분리됨
- [x] **E5** 실행 계획 읽기
- [ ] **E6** BLOB 직렬화 저장 vs 컬럼 펼치기
- [ ] **E7** 대량 삭제와 retention 운영
- [ ] **E8** 커넥션 풀
- [ ] **E9** SQL 방언(dialect)과 이식성 — 확장됨(범위 넓어짐, 복습 시 새 관점 추가)
- [ ] **E10** 스키마 마이그레이션

## I. 설계·아키텍처
- [x] **I1** 응집도와 결합도
- [ ] **I2** DRY 원칙과 범위
- [ ] **I3** SOLID 원칙 개요
- [x] **I4** 의존성 주입(DI)과 테스트 용이성
- [ ] **I5** Singleton vs Dependency Injection
- [ ] **I6** 전역 상태(static) 안티패턴
- [ ] **I7** 관심사의 분리·계층화
- [ ] **I8** 재활용 원칙 — 원천 값
- [ ] **I9** 지연 로딩과 지연 초기화
- [ ] **I10** 레거시 코드 현대화·마이그레이션

## J. 테스트·품질
- [ ] **J1** 테스트 더블과 단위 테스트 격리
- [ ] **J2** 테스트 피라미드
- [ ] **J3** Mockito 기본 3단계
- [ ] **J4** Mockito 인자 검증 — Matcher와 Captor
- [ ] **J5** 상태 검증 vs 행위 검증
- [ ] **J6** 추상 클래스·인터페이스 테스트 전략
- [ ] **J7** Mock 생명주기 관리
- [ ] **J8** 통합 테스트
- [ ] **J9** 테스트 커버리지의 의미와 함정

## L. 버전 관리(Git)
- [ ] **L1** Git 상태 모델과 선별 스테이징
- [ ] **L2** .gitignore 패턴과 3계층 무시
- [ ] **L3** 커밋 위생
- [ ] **L4** git pull과 동기화 원리
- [ ] **L5** 브랜치 전략
- [ ] **L6** rebase vs merge
- [ ] **L7** 되돌리기 — revert·reset·bisect

## M. 빌드·패키지·배포
- [ ] **M1** 패키지 의존성 3층 구조
- [ ] **M2** 빌드 방식 vs dev 서버(HMR)
- [ ] **M3** 빌드 번들 검사 방법론
- [ ] **M4** 정적 자산 캐싱과 무효화 — 확장됨(범위 넓어짐, 복습 시 새 관점 추가)
- [ ] **M5** 환경별 빌드·설정 분리
- [ ] **M6** CI/CD 파이프라인 개념

## O. 운영·인프라·디버깅
- [ ] **O1** 환경변수 수명주기와 12-Factor
- [ ] **O2** 정적 파일 서빙 모델
- [ ] **O3** 정적 상태 vs 동적 상태 디버깅
- [ ] **O4** 헬스체크와 프로세스 관리
- [ ] **O5** 장애 대응 기초

## P. 관측성·모니터링
- [x] **P1** 로깅과 관측성 기초
- [ ] **P2** JMX 기반 운영 모니터링

## S. 보안
- [x] **S1** 프론트엔드 보안 기초
- [ ] **S2** 토큰 기반 인증 — JWT vs 세션 — 분리됨
- [ ] **S7** OAuth2·OIDC 위임 인가 — 분리됨
